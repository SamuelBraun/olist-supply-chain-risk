# This repository tracks ONLY the final code submission folder.
# Everything else (working src/, notebooks/, presentation/, docs/, etc.)
# is kept on local disk but intentionally untracked.
/*
!/.gitignore
!/code_submission/
