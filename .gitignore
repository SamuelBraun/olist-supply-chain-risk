# This repository tracks ONLY the final code submission folder plus the
# management presentation PDF. Everything else (working src/, notebooks/,
# presentation sources, docs/, etc.) is kept on local disk but untracked.
/*
!/.gitignore
!/code_submission/
!/BDA_current_draft.pdf
